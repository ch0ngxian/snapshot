# Snapshot

Mobile multiplayer photo-tag game. Players in the same physical area join a lobby; during a round they tag each other by taking a photo with their phone camera, which deducts a life from the photographed player. Last player(s) alive win.

## Status

v1 MVP — friends/family scope. See [`tech-plan.md`](./tech-plan.md) for the full plan, key decisions, and milestones.

## Stack

- **Client:** Flutter (iOS + Android, single codebase)
- **Backend:** Firebase (Auth, Firestore, Cloud Functions, Storage, FCM, Remote Config) — region `asia-southeast1`, project `cx-snapshot`
- **Face pipeline (on-device):** ML Kit Face Detection (detect & crop) → MobileFaceNet via TFLite (embed)

## Local development

### Prerequisites

- Flutter SDK 3.38.x stable (`flutter --version`)
- Node.js 20+ (for Cloud Functions)
- Firebase CLI (`npm install -g firebase-tools`)
- FlutterFire CLI (`dart pub global activate flutterfire_cli`) — adds `flutterfire` to `~/.pub-cache/bin`
- Xcode (for iOS builds) / Android Studio + SDK (for Android builds)

### First-time setup after cloning

The `google-services.json` (Android) and `GoogleService-Info.plist` (iOS) are gitignored. Each developer regenerates them locally:

```bash
firebase login
firebase use cx-snapshot
~/.pub-cache/bin/flutterfire configure --project=cx-snapshot
```

This refreshes `lib/firebase_options.dart` and the two platform config files.

### Run the Flutter app

```bash
flutter pub get
flutter run
```

The app signs in anonymously, then either runs onboarding (first launch) or shows a placeholder home (subsequent launches). The face embedder is currently `FakeFaceEmbedder` — see TODO in `lib/main.dart`; swap to `MobileFaceNetEmbedder.create()` once the model asset is sourced.

### Tests

```bash
# Flutter unit + widget tests
flutter analyze
flutter test

# Cloud Functions tests
cd functions
npm install
npm test
```

### Local Firebase emulator

```bash
firebase emulators:start
```

Auth (9099), Firestore (8080), Functions (5001), Storage (9199), and the Emulator UI are all wired up via `firebase.json`.

## Repository layout

```
lib/
  face/                 — FaceEmbedder interface + MobileFaceNet + ML Kit pipeline
  models/               — Plain-data classes (UserProfile, …)
  onboarding/           — Three-screen onboarding flow + orchestrator
  services/             — Service interfaces, test fakes, Firebase concrete impls
  firebase_options.dart — generated by `flutterfire configure`
functions/              — TypeScript Cloud Functions
  src/deleteUserData.ts — per tech-plan.md §5.10
firestore.rules         — Firestore security rules (Phase 0: users only)
storage.rules           — Cloud Storage rules (Phase 0: selfies only)
firestore.indexes.json  — composite index on tags.resolvedTargetUid
remoteconfig.template.json — tag_match_threshold + borderline_half_width defaults
firebase.json           — emulator + deploy + flutter platform config
tools/fetch_model.sh    — sourcing script for the MobileFaceNet TFLite asset
```

## Phase 0 — what's done, what's left

**Done in code:**

- ✅ Flutter scaffold + CI (PR #1)
- ✅ On-device face pipeline (ML Kit + MobileFaceNet wiring) (PR #2)
- ✅ Onboarding UI + Cloud Function + security rules + Remote Config template (PR #3)
- ✅ Firebase wiring: deps, concrete services, `firebase_options.dart`, build config (this PR)

**Remaining manual followups:**

- [ ] **Deploy:** `firebase deploy --only firestore:rules,storage:rules,firestore:indexes,remoteconfig,functions`
- [ ] **Storage lifecycle rule**: configure the 30-day auto-delete on the `tags/` prefix in the GCS console (per §5.9; not expressible in `storage.rules`).
- [ ] **Cloud Functions enabled / Blaze plan**: required before `firebase deploy --only functions` works.
- [ ] **MobileFaceNet asset**: pin a verified Apache-2.0 mirror in `tools/fetch_model.sh` (`MODEL_URL` + `MODEL_SHA256`) and drop the binary at `assets/models/mobilefacenet.tflite`. Then swap `FakeFaceEmbedder()` in `lib/main.dart` for `await MobileFaceNetEmbedder.create()`.
- [ ] **Latency gate**: run on a low-end Android (Pixel 4a / Galaxy A-series). On-device pipeline p95 must be <300ms. If it isn't, swap to the quantized embedder per §5.7.

## License

TBD.
